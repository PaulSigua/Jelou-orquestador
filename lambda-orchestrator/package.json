{
  "name": "lambda-orchestrator",
  "version": "1.0.0",
  "description": "Función Lambda para orquestar la creación de pedidos",
  "main": "src/handler.js",
  "scripts": {
    "build": "echo \"Build step not needed for simple JS Lambda\"",
    "dev": "serverless offline --httpPort 3003",
    "deploy": "serverless deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "joi": "^17.13.1"
  },
  "devDependencies": {
    "serverless": "^3.40.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-offline": "^13.3.4"
  }
}
